const title = 'Mój Sklep Next Shop';
const description =
    'Aplikacja Testowa powstała w celu zrozumienia działania Next.js';

const nextSeoConfig = {
    title,
    description,
    openGraph: {
        title,
        description,
        site_name: 'Next Shop',
    },
};

export default nextSeoConfig;
